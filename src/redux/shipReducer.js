let initialState = {
    ships: [],
    pageSize: 10,
    totalUsersCount: 0,
    currentPage: 1,
    isFetching: false,
  };

  const shipReducer = (state = initialState, action) => {
  
}

export default shipReducer;